"""Mock server simulating Verkada Cloud API response shapes. NOT a real Verkada integration."""

# TODO: pydantic models + FastAPI app + 2 endpoints (next prompt)
# Endpoints: GET /camera/v1/info, POST /webhook/access-event
